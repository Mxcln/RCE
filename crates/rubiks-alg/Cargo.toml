[package]
name = "rubiks-alg"
version = "0.1.0"
edition = "2021"

[dependencies]
include_dir = "0.7"
rand = "0.8"
rubiks-core = { path = "../rubiks-core" }
serde = { version = "1", features = ["derive"] }
toml = "0.8"
